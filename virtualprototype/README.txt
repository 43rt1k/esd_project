This work was done entirely by Dominic.
What this project does:
– Grayscale via CI
– Sobel via CI
– Gaussian smoothing via CI
– GPIO for joystick
– GPIO for DIP switch
– GPIO output of DIP switch on seven-segment display
– Profiling via CI
– LED matrix display
– dmaRam (not integrated correctly → not working)
– Some .c structure with the code split into different files
– A very poorly working snake game (main issue is in the .c code)